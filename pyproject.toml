[project]
name = "ibl_datoviz"
version = "0.2.0.dev0"
description = "Datoviz v0.4 visualization components for IBL atlas data"
authors = [
    {name = "International Brain Laboratory", email = "software-devs@internationalbrainlab.org"},
]

requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

dependencies = [
    "datoviz>=0.4.0rc2,<0.5",
    "ibl-atlas-assets>=0.0.0",
    "numpy>=1.24",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv.sources]
# Immutable revisions define the spike. Adjacent source checkouts may override via PYTHONPATH.
datoviz = { git = "https://github.com/datoviz/datoviz.git", rev = "1114b65fa1b7c4be088d7546adf6ae3830a04fab" }
ibl-atlas-assets = { git = "https://github.com/int-brain-lab/ibl-atlas-assets.git", rev = "f5096c3a9c850a3114e13dc6ba9f4d9259e89f04" }

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.12.10",
]

[tool.ruff]
line-length = 99
include = ["pyproject.toml", "ibl_datoviz/**/*.py", "examples/**/*.py", "tests/**/*.py"]

[tool.ruff.format]
quote-style = "single"

[tool.ruff.lint]
ignore = [
  "D100",    # Missing docstring in public module
  "D104",    # Missing docstring in public package
  "PLR2004", # Magic value used in comparison
  "PLR0913", # Too many arguments
]

select = [
  "A",    # flake8-builtins
  "B",    # flake8-bugbear
  "ICN",  # flake8-import-conventions
  "LOG",  # flake8-logging
  "PIE",  # flake8-pie
  "SIM",  # flake8-simplify
  "TID",  # flake8-tidy-imports
  "TC",   # flake8-type-checking
  "E",    # pycodestyle Error
  "W",    # pycodestyle Warning
  "F",    # Pyflakes
  "I",    # isort
  "N",    # pep8-naming
  "PL",   # pylint
  "UP",   # pyupgrade
  "D",    # pydocstyle
  "FURB", # refurb
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D"] # pydocstyle

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.isort]
known-first-party = ["ibl*"]
