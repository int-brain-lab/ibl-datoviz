[project]
name = "ibl_datoviz"
version = "0.2.0.dev0"
description = "Datoviz v0.4 visualization components for IBL atlas data"
authors = [
    {name = "International Brain Laboratory", email = "software-devs@internationalbrainlab.org"},
]

requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

dependencies = [
    "datoviz>=0.4.0rc2,<0.5",
    "ibl-anatomy>=0.1.0,<0.2",
    "numpy>=1.24",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv.sources]
# Immutable revisions define the spike. Adjacent source checkouts may override via PYTHONPATH.
datoviz = { git = "https://github.com/datoviz/datoviz.git", rev = "3bdfee4e871d27775c471779b5f711723cd22db8" }
ibl-anatomy = { git = "https://github.com/int-brain-lab/ibl-anatomy.git", rev = "c66486c7f38f0751848dde0f12a718c8d49e81aa" }

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.12.10",
]

[tool.ruff]
line-length = 99
include = ["pyproject.toml", "ibl_datoviz/**/*.py", "examples/**/*.py", "tests/**/*.py"]

[tool.ruff.format]
quote-style = "single"

[tool.ruff.lint]
ignore = [
  "D100",    # Missing docstring in public module
  "D104",    # Missing docstring in public package
  "PLR2004", # Magic value used in comparison
  "PLR0913", # Too many arguments
]

select = [
  "A",    # flake8-builtins
  "B",    # flake8-bugbear
  "ICN",  # flake8-import-conventions
  "LOG",  # flake8-logging
  "PIE",  # flake8-pie
  "SIM",  # flake8-simplify
  "TID",  # flake8-tidy-imports
  "TC",   # flake8-type-checking
  "E",    # pycodestyle Error
  "W",    # pycodestyle Warning
  "F",    # Pyflakes
  "I",    # isort
  "N",    # pep8-naming
  "PL",   # pylint
  "UP",   # pyupgrade
  "D",    # pydocstyle
  "FURB", # refurb
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["D"] # pydocstyle

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.isort]
known-first-party = ["ibl*"]
